# Crossref Test

## API

- `npm run dev`
- `/articles?offset=0&limit=10` returns publications ordered by doi, with offset & limit query parameters

## Seed the Db

- `npm run seed`

## Jest Testing

- `npm run test`
